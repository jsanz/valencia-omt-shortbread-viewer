{
  "name": "valencia-viewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite src",
    "prebuild": "rimraf dist",
    "build": "vite build src --outDir ../dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "vite": "^4.3.2"
  },
  "dependencies": {
    "maplibre-gl": "^2.4.0",
    "pmtiles": "^2.7.2"
  }
}
